<Window x:Class="TerraGeo.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="O programu" Height="340" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBg}"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="TerraGeo Inženjering"
                   FontSize="26" FontWeight="Bold"
                   Foreground="{StaticResource Accent}"/>
        <TextBlock Grid.Row="1" Text="Designed by Vladimir Djokich"
                   FontSize="14" Margin="0,6,0,0"
                   Foreground="{StaticResource Fg}"/>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <TextBlock x:Name="VersionText" Foreground="{StaticResource Fg}"/>
            <TextBlock x:Name="CopyrightText" Margin="0,8,0,0"
                       Foreground="{StaticResource Fg}"/>
            <TextBlock Margin="0,8,0,0" TextWrapping="Wrap"
                       Foreground="{StaticResource Fg}"
                       Text="Profesionalna geodetska platforma. Faza 1: projekti, tačke, SQLite baza, branding, Helmert 7-param transformacija."/>
        </StackPanel>

        <Button Grid.Row="3" Content="Zatvori" Width="100"
                HorizontalAlignment="Right" Click="OnClose"/>
    </Grid>
</Window>
